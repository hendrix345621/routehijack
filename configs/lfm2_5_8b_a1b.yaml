extends: base.yaml

# Analysis-only profile: the suffix optimizer does not support its biased sigmoid gate.
model:
  hf_id: LiquidAI/LFM2.5-8B-A1B
  n_layers: 24
  n_experts: 32
  top_k: 4
  arch:
    name: lfm2
  routing:
    scoring_func: sigmoid
    use_bias: true
    norm_topk_prob: true
    routed_scaling_factor: 1.0
